table:
  name: company_vendor_contacts
  schema: public
object_relationships:
- name: user
  using:
    foreign_key_constraint_on: vendor_user_id
select_permissions:
- permission:
    columns:
    - id
    - partnership_id
    - vendor_user_id
    filter: {}
  role: bank_admin
- permission:
    columns:
    - id
    - partnership_id
    - vendor_user_id
    filter: {}
  role: bank_read_only
