CREATE TABLE "public"."bespoke_catalog_skus" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "sku" text NOT NULL, "bespoke_catalog_brand_id" uuid NOT NULL, "is_deleted" bool NOT NULL DEFAULT false, "link" text, "picture" text, "created_at" timestamptz NOT NULL DEFAULT now(), "updated_at" timestamptz NOT NULL DEFAULT now(), "deleted_at" timestamptz, PRIMARY KEY ("id") , FOREIGN KEY ("bespoke_catalog_brand_id") REFERENCES "public"."bespoke_catalog_brands"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("id"), UNIQUE ("sku"));
