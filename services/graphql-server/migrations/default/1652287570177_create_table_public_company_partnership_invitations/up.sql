CREATE TABLE "public"."company_partnership_invitations" ("id" uuid NOT NULL, "email" text NOT NULL, "requesting_company_id" uuid NOT NULL, "requested_at" timestamptz NOT NULL, "closed_at" timestamptz, "metadata_info" json NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), "updated_at" timestamptz NOT NULL DEFAULT now(), "deleted_at" timestamptz, PRIMARY KEY ("id") , FOREIGN KEY ("requesting_company_id") REFERENCES "public"."companies"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("id"));
