CREATE EXTENSION IF NOT EXISTS pgcrypto;
CREATE TABLE "public"."metrc_sales_transactions"("id" uuid NOT NULL DEFAULT gen_random_uuid(), "type" text NOT NULL, "company_id" uuid NOT NULL, "package_id" text NOT NULL, "package_label" text NOT NULL, "product_name" text NOT NULL, "quantity_sold" numeric NOT NULL, "unit_of_measure" text NOT NULL, "total_price" numeric NOT NULL, "recorded_datetime" timestamptz NOT NULL, "payload" json NOT NULL, "last_modified_at" timestamptz NOT NULL, "query_start_date" date NOT NULL, "query_end_date" date NOT NULL, "product_category_name" text NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("company_id") REFERENCES "public"."companies"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("id"));
