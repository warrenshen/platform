query GetPayment($id: uuid!) {
  payments_by_pk(id: $id) {
    id
    ...Payment
    company {
      id
      name
    }
    submitted_by_user {
      id
      full_name
    }
  }
}

query GetPaymentForSettlement($id: uuid!) {
  payments_by_pk(id: $id) {
    id
    ...Payment
    company {
      id
      name
      contract {
        id
        product_type
      }
    }
    submitted_by_user {
      id
      full_name
    }
  }
}
