query GetPayment($id: uuid!) {
  payments_by_pk(id: $id) {
    id
    ...PaymentLimited
    company {
      id
      name
    }
    submitted_by_user {
      id
      full_name
    }
  }
}

query GetPaymentForSettlement($id: uuid!) {
  payments_by_pk(id: $id) {
    id
    ...Payment
    company {
      id
      name
      contract {
        id
        ...Contract
      }
    }
    submitted_by_user {
      id
      full_name
    }
  }
}
