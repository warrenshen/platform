query AllPurchaseOrderLoansForBank {
  loans(where: { loan_type: { _eq: purchase_order } }) {
    id
    ...Loan
    purchase_order {
      id
      order_number
      company {
        name
      }
      vendor {
        name
      }
    }
  }
}
