query PurchaseOrderLoansForBank {
  loans(where: { loan_type: { _eq: purchase_order } }) {
    id
    ...Loan
    company {
      name
    }
    purchase_order {
      id
      order_number
      vendor {
        name
      }
    }
  }
}
